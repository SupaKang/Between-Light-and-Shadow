// Shared UI constants (640x360 layer).
#pragma once
#include "state.h"

namespace yy {

// ---------------------------------------------------------------- rendering: UI (640x360 layer)
const Color TXT = UI_TEXT, TXT_DIM = UI_DIM, TXT_RED = UI_GLOW, TXT_LIGHT = UI_TEXT;  // TXT_RED = accent
constexpr int DX0 = 16, DY0 = 288, DW = 608, DH = 60;  // dialog box
}  // namespace yy

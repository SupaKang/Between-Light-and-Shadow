# BLS Dev Log — 2026-09-23

## 문서 체계 정비
devlog와 decisions 문서 폴더를 새로 만들었습니다. nosync와 graft 캐시 폴더는 git에서 제외했습니다.

## 코드 품질 검사 도입
C++ 코드를 AI 리뷰에 앞서 기계적으로 먼저 검사하도록 기반을 깔았습니다.

- **컴파일러 경고:** Mac 빌드에 `-Wall -Wextra` 경고를 켰습니다. 경고는 0개였습니다. `compile_commands.json`도 생성하도록 했습니다.
- **메모리 오류 검출:** `BLS_SANITIZE` 옵션을 켜면 ASan+UBSan으로 빌드됩니다(`build-asan/`). `BattleRulesTest`가 이 빌드에서 통과했습니다.
- **정적 분석:** clang-tidy와 cppcheck를 설치하고 `.clang-tidy` 규칙 파일을 추가했습니다. 잡음이 심한 규칙 4개는 껐습니다.
- **검사 스크립트:** `tools/check.sh`는 경고 빌드, sanitizer 테스트, 변경 파일 정적 분석을 한 번에 돌립니다. `--all`로 전체 검사를 하며 약 8초 걸립니다.
- **코드 수정:** `main.cpp`의 34행과 594행에서 곱셈 결과가 넓은 타입으로 바뀌는 부분에 `static_cast`를 넣었습니다.

## Claude Code 개발 환경 개선
토큰 절감과 작업 흐름을 개선할 도구를 넣고 설정을 정리했습니다.

- **중복 hook 제거:** graft hook이 전역과 프로젝트 양쪽에 등록되어 두 번씩 실행되고 있었습니다. 프로젝트 쪽을 지웠습니다.
- **새 도구:** ccusage(사용량 측정), context-mode(큰 출력 처리, 이 프로젝트만), claude-hud(statusline), open-code-review(`ocr` 위임 모드와 플러그인)
- **OVERMIND에서 가져온 스킬:** devlog, handoff, improve-codebase, `/cp`. 저장 경로를 이 프로젝트에 맞게 바꿨고, `/cp`는 커밋 전에 `tools/check.sh`를 실행합니다.

### 변경 파일
- `CMakeLists.txt`, `.clang-tidy`, `.gitignore`, `tools/check.sh`, `src/main.cpp`
- `.claude/skills/{devlog,handoff,improve-codebase}/`, `.claude/commands/cp.md`

## 의사결정
- **rtk 도입 안 함:** JetBrains 벤치마크에서 실제 절감이 0이었고, 비용이 오히려 7.6% 늘어난 경우도 있었습니다.
- **ASan 빌드는 Homebrew LLVM clang 사용:** Apple clang 17의 ASan은 macOS 26에서 시작하다 멈춥니다.
- **context-mode와 ocr 플러그인은 local 범위로 설치:** 이 프로젝트에서만 효과를 측정하기 위해서입니다.
- **정적 분석을 AI 리뷰보다 먼저 적용:** C++ 메모리 버그는 결과가 정해진 도구가 더 정확하고 토큰도 쓰지 않습니다.

## 블로커 & 이슈
- [ ] context-mode의 `better-sqlite3`를 수동으로 설치했습니다. 플러그인을 업데이트하면 빠지므로 `/context-mode:ctx-doctor`로 다시 확인해야 합니다.
- [ ] 컴파일러 경고 옵션은 Mac/Linux 빌드에만 적용됩니다. MSVC 빌드는 원래의 `/W4`를 그대로 씁니다.

## 다음 스텝
- [ ] 며칠 사용한 뒤 `ccusage daily`로 오늘 값(860만 토큰, $4.94)과 비교하고, context-mode를 계속 쓸지 판단합니다.
- [ ] `/claude-hud:setup`으로 statusline을 교체합니다.
- [ ] `/open-code-review:delegate-review`로 첫 리뷰를 돌려봅니다.
- [ ] 중복 AI 리뷰 도구를 정리합니다(`/review`, `caveman-review`, superpowers의 `requesting-code-review`).
- [ ] 브랜치와 PR로 작업하게 되면 GitHub PR 리뷰 봇을 붙입니다.
